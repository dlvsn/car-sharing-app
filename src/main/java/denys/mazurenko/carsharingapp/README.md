Коментар від Renat Safarov

Привіт! Чудова робота, нижче опишу моменти, які можна покращити:

Давай приберемо @ToString над модельками, щоб в нас не виникало несподіваних багів
На майбутне, з контролерів в сервіси краще передавати об'єкт User замість Authentication, 
бо якщо ти захочеш викликати певний метод сервісу з іншого сервісу, це буде зробити 
трохи складніше + не дуже логічно) --------> Пофіксив

Бачу, що в CustomGlobalExceptionHandler ти обробляєш 2 помилки EntityNotFoundException, 
хоча та що jakarta.persistence.EntityNotFoundException ніде не використовується, 
тож давай видалимо зайвий метод. --------> Пофіксив

Постав над всіма сервісами (рівень класу), що читають/пишуть в БД анотацію @Transactional ,
бо без цього при кожному запиті в репозиторій створюється новий EntityManager/Session 
та транзакція в БД. Це погано з точки зору перформансу (зайві запити) + консистентності даних (різні транзакції).
--------> Пофіксив


Відправку телеграм сповіщень як раз таки краще виносити з транзакції і 
робити це асинхроно (в іншому потоці), щоб робота телеграм серверів не впливало на те, 
що отримає твій клієнт у відповідь при створені замовлення, оплаті і т.п. Ми не хочемо, 
щоб це впливало на роботу нашої системи і того, чи збережуться дані в БД. 
Раджу глянути ось цей тред, де Лілія дуже класно описала чому це важливо та як це можна пофіксити. 
Спробуй розібратися (не копіюючи рішення) і реалізувати це в себе:**** 
--------> Додав анотацію async. Логував потоки, наче все ок

В TelegramBotMessages можеш зробити константи з повідомленнями public і прибрати @Getter. 
------> Вирішив заюзати білдер, надіюсь що виглядає не жахливо )))) На поля паблік поставити не отрималось
бо чекстайл почав ругатись за модифікатор, тому приняв рішення зробити типу єнам + білдер

Загалом помітив, що логіка відправки сповіщень в тебе більше і складніше ніж вся інша частина додатку. 
Досить важко зрозуміти, що відбувається) Чи впевнений, що ти нічого не ускладнюєш? 
------>  Знайшов один гайд в гуглі розібрався, і вирішив зробити так, в класі де описаний бот, залишив коментарі. 
Багато гайдів які знаходив, переважно всі екстендились від TelegramLongPollingBot але він deprecated, 
не знаю на справді, чи принципово уникати deprecated класи/методи, тому вирішив зробити бота таким шляхом.  

Окремо хочу відмітити, що дуже класно 
написані контролери (з описом, статус кодами), 
модельки (всюди fetchType = LAZY, вкладені енами + використав @MapsId та поставив contraint unique, де треба) 
та методи в репозиторія (використав @Query + JOIN FETCH та @EntityGraph для підгрузки додаткових даних) 
----------> ВЕЛИКЕ ДЯКУЮ ЗА ПОХВАЛУ !!!!!)))))